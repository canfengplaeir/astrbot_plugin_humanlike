# 拟人化群聊助手（astrbot_plugin_humanlike）

一个 AstrBot 插件：让机器人在群聊中按心流和 AI 判断决定是否发言，而不是每条消息都回复。

插件不管理人格。机器人的性格、名字、语气全部来自 AstrBot 当前选中的默认人格，插件只决定三件事：何时说、说多少、要不要主动说。

## 功能

### 心流引擎

每个群独立维护一个 0-100 的"参与意愿"值，随时间自然衰减，被刺激时上升。以下为「日常闲聊群」预设的默认值，全部可调。

上升：

| 触发条件 | 加成 | 说明 |
|---|---|---|
| 被 @ | +35 | 直接叫到机器人 |
| 人格名被提到 | +35 | 名字出现在消息里 |
| 匹配关键词 | +18 | 配置的兴趣话题 |
| 消息含问号 | +20 | 可能有人在提问 |
| 群聊活跃 | +10 | 60 秒内多条消息 |
| 回复互动 | +15 | 机器人发言后 30 秒内被叫名字 |

下降：

| 触发条件 | 衰减量 |
|---|---|
| 自然流失 | 每秒 -0.03 |
| 发言后 | -18 |
| 同一人连续发言 | 第 3 条起每条 -3 |

### 消息累积

消息多的时候先不回复，等群里安静下来再一起看，模拟人看手机的习惯。计时器带 ±30% 随机抖动。

- 立即处理：被 @、被叫人格名、心流 ≥ 70，跳过等待直接判断
- 累积等待：普通消息进缓冲，安静 15±30% 秒后批量处理；缓冲满 15 条强制处理

### 防抖

- 动态冷却：心流高时冷却短（4 秒），低时冷却长（10 秒）
- 频率限制：默认每 60 秒最多回复 12 次
- 随机沉默：默认 10% 概率保持沉默
- 被点名（@）时豁免全部防抖检查，必定回复

### AI 判断与回复

两级 LLM 调用：先判断是否发言，通过后再生成回复。判断和回复可以配置不同的模型。所有 LLM 调用都有超时（判断 12 秒 / 回复 45 秒 / 关键词 30 秒 / 主动 25 秒），超时或失败按沉默处理，不会卡住流程。

@ 归属处理：提示词会明确标注消息的 @ 对象——「@了你」「@了其他成员」「@全体成员」；判断提示词固定附加"消息归属"原则。被点名时跳过 AI 判断直接回复，@ 别人的消息正常走判断流程，AI 不会误以为在叫自己。

### 主动发言

群聊冷场超过阈值后，由 AI 结合人格和最近话题决定是否主动开口。默认关闭。

| 配置 | 默认 | 说明 |
|---|---|---|
| 静默阈值 | 20 分钟 | 群聊多久没消息后考虑发言 |
| 主动冷却 | 60 分钟 | 两次主动发言的最小间隔 |
| 最低心流 | 45 | 低于此值不主动开口 |
| 每日上限 | 3 次/群 | 防止话痨 |

生成话题期间群里若恢复活跃，会自动取消发言。

### 关键词

手动配置的关键词 + AI 根据人格自动生成的关键词。`use_ai_keywords` 开关控制 AI 生成的关键词是否参与匹配。Dashboard 提供关键词测试器。

### 一键配置

三套联动参数预设，一键套用：

| 预设 | 适用场景 |
|---|---|
| 日常闲聊群 | 50~200 人的日常群（默认） |
| 热闹大群 | 200 人以上的活跃群 |
| 安静小群 | 潜水群、小群 |

预设只改数值参数，不碰开关、名单、提示词、模型选择。应用后仍是普通配置，可以继续手动微调。

## 指令

| 指令 | 说明 |
|---|---|
| `/mindflow` | 查看当前群状态：心流值、上次发言、缓冲 |
| `/mindflowreset` | 重置当前群状态 |
| `/keywords` | 查看当前生效的关键词 |
| `/genkeywords` | 让 AI 按人格生成关键词 |

## Dashboard

插件自带一个 Dashboard 页面，三个面板：

- 插件设置：一键配置 + 全部参数可视化编辑（开关、滑块、下拉、提示词、判断模型、超时）
- 关键词管理：手动增删、AI 生成（可选参考人格，兼容 v2/v3 人格系统）、实时测试
- 状态面板：各群心流、发言统计、最近消息、缓冲数，10 秒自动刷新

## 常见问题

**在群里 @ 别人，机器人会回复吗？**

不会误回。插件会识别 @ 的对象：点名机器人时必定回复；@ 别人时按正常流程判断，提示词里会写明「@了其他成员」，AI 只在内容明显需要它参与时才发言。

**调试日志看不到插件的输出？**

插件日志级别独立于 AstrBot 全局日志。排查问题时打开插件设置里的"调试模式"，插件会以 INFO 级别输出 @ 检测详情和消息描述。

**主动发言为什么一直不触发？**

先确认已开启"启用主动发言"。触发条件：群冷场超过静默阈值、心流达到最低值、当天发言次数未达上限。心流不足时（群长期不活跃）不会主动开口，这是有意设计的。

**换了人格没生效？**

插件 30 秒缓存人格设定，最多等半分钟；也可以重载插件立即生效。

## 代码结构

```
astrbot_plugin_humanlike/
├── main.py              # 插件入口：事件处理、指令、流程调度、预设、Web API
├── metadata.yaml        # 插件元数据
├── _conf_schema.json    # 配置项定义
├── engine/              # 纯逻辑层
│   ├── state.py         # 群状态数据结构
│   ├── flow.py          # 心流计算、@/名字检测
│   ├── debounce.py      # 防抖判断
│   └── accumulator.py   # 消息累积、随机抖动计时器
├── ai/                  # AI 交互层
│   ├── persona.py       # 人格读取（强制继承 + 缓存）
│   └── client.py        # LLM 调用封装
└── pages/
    └── dashboard/       # Dashboard 页面
```

## 更新记录

- **1.6.2**：修复批处理路径的 @ 归属误判。写入上下文/缓冲/历史的文本统一把 `<@openid>` 替换为「@其他成员」，批处理判断时 AI 能正确识别 @ 对象。
- **1.6.1**：新增调试模式开关，以 INFO 级别输出 @ 检测和消息描述日志，便于排查（插件日志级别独立于全局 debug）。
- **1.6.0**：@ 归属还原。提示词标注「@了你 / @了其他成员 / @全体成员」，覆盖 At 组件和 QQ 官方平台的文本 @ 两种形态；@ 检测改为组件优先，@ 别人不再误判为提及。
- **1.5.0**：人格强制继承。移除 inherit_persona 开关和 bot_name 备用名。
- **1.4.1**：移除语气微调方向（reply_style），保留心流高低带来的多说/简短提示。
- **1.4.0**：新增一键配置，三套预设（日常闲聊群 / 热闹大群 / 安静小群），联动调整心流、防抖、累积、主动发言、超时参数。
- **1.3.1**：移除群管理功能（入群审核、防爆破、欢迎）。原因：qq_official 平台收不到入群事件，功能无法生效。
- **1.1.3**：@ 全体不再触发必定回复；指令放行读取 AstrBot 的 wake_prefix；日志降噪；24 小时无消息的群状态自动清理。
- **1.1.2**：修复若干逻辑问题：并发双回复、活跃度统计错误、AI 关键词开关失效、空消息触发 AI 等。
- **1.1.1**：被点名时跳过 AI 判断和防抖，必定回复。
- **1.1.0**：主动发言、AI 调用超时、Dashboard 高级设置、人格列表兼容 v2/v3。
